FIB

•	Write a non recursive function that prints the 10 first numbers of the Fibonacci sequence.

•	What is (are) the advantage(s) of using a non-recursive function?
The advantage of using a non-recursive function is the facility to find errors.
